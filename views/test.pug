extends layout

block content

	ul
	each val, index in ['zero', 'one', 'two']
		li= index + ': ' + val
		
	- var values = [];

	ul
	each val in values.length ? values : [{ "name":"John", "age":30, "car":null },{ "name":"John", "age":30, "car":null }]
		li= val.name
		li= val.age
		li= val.car

